using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PickUpV2 : MonoBehaviour {
    //idea: get all interactable objects in a certain area. The closest one is the one that is picked. This prevents a person from picking up multiple items.
	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
